********** Modelo de Roles *********

import { Schema, model } from "mongoose";

const roleSchema = new Schema(
  {
    name: String,
  },
  {
    versionKey: false,
  }
);

export default model("Role", roleSchema);

Ahora vamos a crear el controller de auth.controller.js

